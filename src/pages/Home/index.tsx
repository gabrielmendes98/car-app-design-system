import Button from 'components/Button';
import Input from 'components/Input';

const Home = () => (
  <section>
    <h1>Home page</h1>
    <Input label="test" name="name" />
    <Button>dewde</Button>
  </section>
);

export default Home;
